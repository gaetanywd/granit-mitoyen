# Variables d'environnement (optionnel). .env n'est pas versionné (voir .gitignore).
# Lien boutique BigCartel affiché dans le header si défini.
PUBLIC_BIGCARTEL_URL=https://granit-mitoyen.bigcartel.com/
