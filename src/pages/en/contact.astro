---
// Contact page (EN) : main content loaded from an editable JSON file. No form.
import Layout from '../../layouts/Layout.astro';
import { contactLinks, CONTACT_EMAIL } from '../../data/contact';
import content from '../../../content/contact.en.json';

const { meta, hero, intro } = content;
---

<Layout title={meta.title} description={meta.description}>
  <h1 class="font-title text-3xl font-bold mb-6">
    {hero.title}
  </h1>

  {hero.subtitle && (
    <p class="font-subtitle mb-4 text-gray-600 max-w-xl">
      {hero.subtitle}
    </p>
  )}

  {intro?.text && (
    <p class="font-body mb-6 text-gray-700 max-w-xl">
      {intro.text}
    </p>
  )}

  <p class="font-body mb-4 max-w-xl">
    Email —{' '}
    <a href={`mailto:${CONTACT_EMAIL}`} class="text-blue-600 hover:text-blue-800 hover:underline">
      {CONTACT_EMAIL}
    </a>
  </p>

  <ul class="space-y-3 max-w-xl">
    {contactLinks.map((link) => (
      <li>
        <a
          href={link.url}
          target="_blank"
          rel="noopener noreferrer"
          class="text-blue-600 hover:text-blue-800 hover:underline"
        >
          {link.type.charAt(0).toUpperCase() + link.type.slice(1)}
        </a>
      </li>
    ))}
  </ul>
</Layout>
